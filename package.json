{
  "name": "native",
  "esy": {
    "build": "dune build -p #{self.name}",
    "release": {
      "releasedBinaries": [
        "native"
      ]
    }
  },
  "devDependencies": {
    "@opam/merlin": "*",
    "@opam/dune": "*",
    "@opam/lwt": "*",
    "ocaml": "~4.6.0"
  }
}
